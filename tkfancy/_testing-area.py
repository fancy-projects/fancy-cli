"TEST 1: Custom Entry Widget with Rounded Border"


# from PIL import Image, ImageDraw, ImageTk
# import tkinter as tk

# def create_rounded_entry(root):
#     """Creates a custom entry widget with a rounded border."""
#     # ... (rest of the code for creating the rounded entry)
 

"TEST 2: Filtering Dictionary with Non-None Values"

# side_options = {key: side_options[key] for key in side_options if side_options[key] is not None}
# side_options = filter(lambda side: side_options[side] is not None, side_options)  # filter function
# side_options = filter(lambda side: side[1] is not None, side_options.items())  # filter with items()
